/*
  if (inputNormalized.length < 1) {
    listIngredient.innerHTML = ingredientDropDownContent;
    const ingredientItem = document.querySelectorAll(
      '.search__filter__list__item'
    );
    selectIngredientTag(ingredientItem);
  } else {
    searchIngredient(globalSearch, inputNormalized);
    const ingredientItem = document.querySelectorAll(
      '.search__filter__list__item'
    );
    selectIngredientTag(ingredientItem);
  } */

// const ingredientsOfRecipes = displayIngredient(globalSearch).join('');
// listIngredient.innerHTML = ingredientsOfRecipes;
// ingredientDropDownContent = ingredientsOfRecipes;

/*

function displayIngredient(arr) {
  const ingredientResult = arr.flatMap((element) => {
    const allIngredients = element.ingredients;

    const ingredients = allIngredients.map((el) => {
      const { ingredient } = el;
      return `<li class="search__filter__list__item --ingredient">${ingredient}</li>`;
    });
    return ingredients;
  });
  return removeDuplicate(ingredientResult);
}
      */
